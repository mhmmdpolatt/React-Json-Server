function Course({course , remove}) {
    // console.log(course.id);
    // console.log(course.title);

    return ( 
    <>
    
        {/* <div className="card">
            <div className="cardTitlePRice">
                <h2>{course.title}</h2>
                <h4>{course.price} TL</h4>
            </div>

            <div className="content">
                <p>{course.content}</p>
            </div>
            <button className="deleteButton" onClick={()=>remove(course.id)}>Sil</button>
        </div> */}
    
    </> 
    
    );
}

export default Course;